export enum Orientation {
    Vertical = 0,
    Horizontal
}

export enum RoomState {
    Waiting = 0,
    Playing = 1
}
export const enum Difficulty {
    Easy = "Easy",
    Normal = "Normal",
    Hard = "Hard"
}